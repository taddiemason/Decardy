name = "decardy-website"
main = "worker.js"
compatibility_date = "2024-09-23"

# If you want to bind to your real domain:
# routes = [
#   { pattern = "decardy.com/*", zone_name = "decardy.com" }
# ]

[assets]
# Serve static files directly from the repo root — no build step required
directory = "."
binding = "ASSETS"
